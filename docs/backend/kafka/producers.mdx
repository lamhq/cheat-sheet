# Producers

## Introduction

Producers are the publisher of messages.

Producers send messages to specific partitions and know in advance which partition will receive each message (the server doesn't decide).
