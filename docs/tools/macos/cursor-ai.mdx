# Cursor AI

## Reset Free Trial

### Method 1

1. Run [Cursor Free VIP](https://github.com/yeongpin/cursor-free-vip) in Terminal:
    ```sh
    curl -fsSL https://raw.githubusercontent.com/yeongpin/cursor-free-vip/main/scripts/install.sh -o install.sh && chmod +x install.sh && ./install.sh
    ```
2. First, close Cursor, type `3` (❌ Close Cursor Application) and enter
3. To reset free trial, type `1` (🔄 Reset Machine ID) and enter
4. If you have the error: `❌ No Write Permission`, repeat step `1` and enter your admin password
5. You should become `Pro Trial` again and by pass the `Trial Account Limit` issue

If this doesn't work, delete your Cursor account, repeat above steps, then re-register and open Cursor.


### Method 2

1. Go to the [Cursor setting page](https://cursor.com/dashboard?tab=settings), delete your account (`Advanced Account Settings` -> `Delete Account`) 
2. Run [Cursor Free Trial Reset Tool](https://github.com/yuaotian/go-cursor-help) script:
    ```sh
    curl -fsSL https://raw.githubusercontent.com/yuaotian/go-cursor-help/master/scripts/install.sh | sudo bash
    ```
3. Re-register an account, log in
4. Open Cursor, log in, become `Pro Trial` again

Note: this method doesn't by pass the `Trial Account Limit` issue.


## References

- [4 Cách Sử Dụng Cursor AI Miễn Phí](https://apidog.com/vi/blog/free-cursor-ai/)
- https://github.com/yuaotian/go-cursor-help
- https://github.com/yeongpin/cursor-free-vip
