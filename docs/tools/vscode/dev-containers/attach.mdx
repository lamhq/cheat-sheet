# Attach to a running container

You can attach VS Code to an already running Docker container, regardless of how it was started.


## Attach to a Docker container

Select **Dev Containers: Attach to Running Container...** from the Command Palette
