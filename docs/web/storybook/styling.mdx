# Styling and CSS

## Add global CSS

Import your css in `.storybook/preview.ts`

```ts title=".storybook/preview.ts"
import '../src/styles/global.css';

const preview: Preview = {
  parameters: {},
};

export default preview;
```
