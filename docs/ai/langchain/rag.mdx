# RAG

LangChain code snippets for Retrieval-Augmented Generation (RAG) applications.

## Agentic RAG

Create a simple RAG agent that uses a tool to fetch relevant documents before answering user queries.

```python
import requests
from langchain.tools import tool
from langchain.chat_models import init_chat_model
from langchain.agents import create_agent

@tool
def fetch_url(url: str) -> str:
    """Fetch text content from a URL"""
    response = requests.get(url, timeout=10.0)
    response.raise_for_status()
    return response.text

system_prompt = """\
Use fetch_url when you need to fetch information from a web-page; quote relevant snippets.
"""

agent = create_agent(
    model="claude-sonnet-4-6",
    tools=[fetch_url], # A tool for retrieval
    system_prompt=system_prompt,
)
```


## Project setup

Follow these steps to set up a RAG project:

1. Register an OpenAI account, add some credit (>= $5).
2. Create a Pinecone index and an API key.
3. Create a Python project configuration file `pyproject.toml` (see below).
4. Create a `.env` file (see below).
5. Install project dependencies.

```bash title=".env"
OPENAI_API_KEY=sk-proj-...

LANGSMITH_TRACING=true
LANGSMITH_ENDPOINT=https://apac.api.smith.langchain.com
LANGSMITH_API_KEY=lsv2_pt_...
LANGSMITH_PROJECT=your-project-name

PINECONE_API_KEY=pcsk_...
PINECONE_INDEX=rag-index
```

```toml title="pyproject.toml"
[project]
name = "rag-demo"
requires-python = ">=3.11"
dependencies = [
    "dotenv>=0.9.9",
    "langchain>=1.3.4",
    "langchain-community>=0.4.2",
    "langchain-openai>=1.2.1",
    "langchain-pinecone>=0.2.13",
]
```

```bash
uv sync
```

## Ingestion

Follow these steps to ingest documents into a vector store:

1. Add reference document to `resources/document.txt`.
2. Create a Python script named `ingest.py`.
3. Run the script to ingest documents into Pinecone index.
4. Verify ingestion in your Pinecone dashboard.

```python title="ingest.py"
import os
from dotenv import load_dotenv
from langchain_community.document_loaders import TextLoader
from langchain_openai import OpenAIEmbeddings
from langchain_pinecone import PineconeVectorStore
from langchain_text_splitters import CharacterTextSplitter

# Load environment variables from .env file
load_dotenv()

print("Ingesting...")
loader = TextLoader("resources/document.txt")
document = loader.load()

print("splitting...")
text_splitter = CharacterTextSplitter(chunk_size=1000, chunk_overlap=0)
texts = text_splitter.split_documents(document)
print(f"created {len(texts)} chunks")

embeddings = OpenAIEmbeddings(model="text-embedding-3-small")

print("ingesting...")
PineconeVectorStore.from_documents(
    texts, embeddings, index_name=os.environ["PINECONE_INDEX"]
)
print("finish")
```

```bash
uv run ingest.py
```

## Retrieval (2-Step RAG)

Follow these steps to perform retrieval:
1. Create a Python script named `main.py`.
2. Run the script to perform retrieval.

```py title="main.py"
import os
from operator import itemgetter
from dotenv import load_dotenv
from langchain_core.output_parsers import StrOutputParser
from langchain_core.prompts import ChatPromptTemplate
from langchain_core.runnables import RunnablePassthrough
from langchain_openai import ChatOpenAI, OpenAIEmbeddings
from langchain_pinecone import PineconeVectorStore

load_dotenv()

print("Initializing components...")

embeddings = OpenAIEmbeddings(model="text-embedding-3-small")
llm = ChatOpenAI(model="gpt-4.1-nano-2025-04-14")

vectorstore = PineconeVectorStore(
    index_name=os.environ["PINECONE_INDEX"], embedding=embeddings
)

retriever = vectorstore.as_retriever(search_kwargs={"k": 3})

prompt_template = ChatPromptTemplate.from_template(
    """Answer the question based only on the following context:

{context}

Question: {question}

Provide a detailed answer:"""
)


def format_docs(docs):
    """Format retrieved documents into a single string."""
    return "\n\n".join(doc.page_content for doc in docs)


print("Retrieving...")

# Query
query = "what is Pinecone in machine learning?"

"""
Create a retrieval chain using LCEL (LangChain Expression Language) 
which can be invoked with {"question": "..."}

Advantages over non-LCEL approach:
- Declarative and composable: Easy to chain operations with pipe operator (|)
- Built-in streaming: chain.stream() works out of the box
- Built-in async: chain.ainvoke() and chain.astream() available
- Batch processing: chain.batch() for multiple inputs
- Type safety: Better integration with LangChain's type system
- Less code: More concise and readable
- Reusable: Chain can be saved, shared, and composed with other chains
- Better debugging: LangChain provides better observability tools
"""
chain_with_lcel = (
    RunnablePassthrough.assign(
        context=itemgetter("question") | retriever | format_docs
    )
    | prompt_template
    | llm
    | StrOutputParser()
)
result_with_lcel = chain_with_lcel.invoke({"question": query})
print("\nAnswer:")
print(result_with_lcel)
```

```bash
uv run main.py
```


## Retrieval (Agentic RAG)

```py title="main.py"
import os
from dotenv import load_dotenv
from langchain.agents import create_agent
from langchain.tools import tool
from langchain_openai import ChatOpenAI, OpenAIEmbeddings
from langchain_pinecone import PineconeVectorStore

load_dotenv()

print("Initializing components...")

embeddings = OpenAIEmbeddings(model="text-embedding-3-small")
model = ChatOpenAI(model="gpt-4.1-nano-2025-04-14")

vectorstore = PineconeVectorStore(
    index_name=os.environ["PINECONE_INDEX"], embedding=embeddings
)


@tool(response_format="content_and_artifact")
def retrieve_context(query: str):
    """Retrieve relevant documentation to help answer user queries."""
    retrieved_docs = vectorstore.as_retriever().invoke(query, k=3)
    serialized = "\n\n".join(
        (
            f"Source: {doc.metadata.get('source', 'Unknown')}\n\nContent: {doc.page_content}"
        )
        for doc in retrieved_docs
    )

    return serialized, retrieved_docs


print("Running agentic RAG...")
query = "what is Pinecone in machine learning?"
system_prompt = (
    "You are a helpful AI assistant that answers questions using documentation. "
    "You have access to a tool that retrieves relevant documentation. "
    "Use the tool to find relevant information before answering questions. "
    "Always cite the sources you use in your answers. "
    "If you cannot find the answer in the retrieved documentation, say so."
)

agent = create_agent(model, tools=[retrieve_context], system_prompt=system_prompt)
messages = [{"role": "user", "content": query}]

response = agent.invoke({"messages": messages})
answer = response["messages"][-1].content

print("\nAnswer:")
print(answer)
```
