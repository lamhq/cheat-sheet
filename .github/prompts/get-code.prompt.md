---
agent: agent
model: Grok Code Fast 1 (copilot)
description: Return all the code for a specific section of a fetched webpage, including import statements, formatted in markdown code blocks.
---
return all the code for the provided section name of the fetched webpage

code include the import statements

code is wrapped in \```{language} title="{filename}"" and \```

before the code block, include a brief description of what the code does, start with an action, do not introduce, wrap symbols and code-related terms with backticks, end with colon